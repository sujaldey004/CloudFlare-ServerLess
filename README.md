To start fresh project run : npm create cloudflare -- { App Name }

Command to deploy your backend on cloudflare is : npx wrangler login 
// This command will redirect to the login page 
And then use command : npm run deploy 
// This command will deploy your codebase


If you want to run it locally use Command : npm run dev
